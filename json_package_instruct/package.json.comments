{
  "name": "npte-quiz-react",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@langchain/langgraph-react": "^0.5.0"
  },
  "devDependencies": {
    "react-scripts": "6.x"
  }
}


{
  "_comment":[
  scripts.start runs your development server at http://localhost:3000 (default React behavior),
  build prepares a production-ready static bundle (placed in frontend/build/),
  dependencies are your versioned UI packages,
  private: true prevents accidental publishing to npm,
  ----,
  frontend/package.json:,
  1. Lives in the frontend directory,
  2. Lists React/UI dependencies and commands for npm/yarn,
  3. Used when you cd frontend and run npm install or npm start,
  ----,
  letsreact.org,
  ----,
  💻 How and Where to Use Each One,
  Step	Directory	Action	                                              Purpose,
     1️⃣	backend/	poetry install or pip install -r requirements.txt	S Starts your FastAPI + LangGraph backend,
     2️⃣	frontend/	npm install (once)	I                                Installs React & streaming UI tools,
     3️⃣	frontend/	npm start	L                                        Launches  chat app UI at localhost:3000,
     4️⃣	backend/	poetry run uv or uvicorn app:app	                 Runs API server at localhost:8000,
     ],
},
